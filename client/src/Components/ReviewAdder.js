import React from 'react'

function ReviewAdder({user, car }) {

    
console.log(user.cars_reviewed.includes(car)   )
console.log(car.cars_reviewed)

    return (
        <div>
            
        </div>
    )
}

export default ReviewAdder
